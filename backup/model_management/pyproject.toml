[project]
name = "model_management"
version = "0.1.0"
description = "Model management"
dependencies = [
    "accelerate>=1.15.0",
    "torch>=2.14.0",
    "transformers>=5.17.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
