[project]
name = "inference-engine"
version = "0.0.0"
requires-python = ">=3.14"
dependencies = [
    "maturin>=1.15.0",
    "model_management",
]

[tool.uv.sources]
model_management = { workspace = true }

[tool.uv.workspace]
members = [
    "model_management"
]
